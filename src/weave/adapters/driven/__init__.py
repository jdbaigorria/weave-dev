"""weave.adapters.driven — Outbound adapters (Weave calls out).

Loom engine (wraps ``loom.build``), state repositories, asset stores, config
sources, metrics sinks. Implement the corresponding :mod:`weave.ports`.
"""
